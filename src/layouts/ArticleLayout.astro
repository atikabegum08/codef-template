---
import { Frontmatter } from "src/misc";
import BaseLayout from "./BaseLayout.astro";

export interface Props {
  content: Frontmatter;
}

const { content } = Astro.props as Props;
const { title, image, imageDescription, description } = content;
const date = content.date;
---

<BaseLayout title={title} description={description}>
  <h1
    class="text-3xl md:text-5xl 2xl:text-6xl mb-[0.8em] font-extrabold mx-auto w-fit text-blue-900"
  >
    {title}
  </h1>

  {
    image ? (
      <Fragment>
        <img
          class="w-10/12 mx-auto aspect-video"
          src={image}
          alt={imageDescription ?? title}
        />
      </Fragment>
    ) : null
  }

  <div class="flex flex-wrap justify-evenly mb-9 mt-2">
    <div>{date}</div>
  </div>
</BaseLayout>

<div class="prose md:prose-lg 2xl:prose-xl px-4 mx-auto prose-pre:font-mono">
  <slot />
</div>
